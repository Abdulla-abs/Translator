# 使用说明：
# 1. 复制本文件为仓库根目录下的 config.properties（与 composeApp 同级）。
# 2. 将占位符替换为真实密钥；切勿将 config.properties 提交到 Git（已写入根 .gitignore）。
# 3. 键名与旧工程 android_trans/config.properties 一致，供运行时 SecretsProvider 等读取。
#
# 单元测试请使用 FakeSecretsProvider（见 composeApp commonTest），勿在 CI 中依赖本文件。

# --- Lingvanex ---
lingvanex.token=YOUR_TOKEN_OR_BEARER_PREFIX

# --- 腾讯机器翻译 TMT（可选区域，默认 ap-guangzhou）---
tencent.secretId=YOUR_SECRET_ID
tencent.secretKey=YOUR_SECRET_KEY
# tencent.region=ap-guangzhou

# --- 百度翻译 ---
baidu.appId=YOUR_APP_ID
baidu.secretKey=YOUR_SECRET_KEY

# --- 有道翻译 ---
youdao.appId=YOUR_APP_ID
youdao.secretKey=YOUR_SECRET_KEY

# --- 火山引擎（OpenAPI）---
huoshan.accessKeyID=YOUR_ACCESS_KEY_ID
huoshan.secretAccessKey=YOUR_SECRET_ACCESS_KEY
